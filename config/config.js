// Swagger Express Configures
const config = {};

config.appRoot = `${process.cwd()}/src/routes`;
config.configDir = `${process.cwd()}/src/routes`;
config.swaggerFile = `${process.cwd()}/src/routes/swagger.json`;

module.exports = config;
